{
  "name": "gameoftheyear",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:all": "start \"json-server\" cmd /k \"cd /d %cd% && npm.cmd run server\" && vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "server": "json-server --watch data.json --port 3000"
  },
  "devDependencies": {
    "json-server": "^1.0.0-beta.15",
    "sass": "^1.99.0",
    "typescript": "~6.0.2",
    "vite": "^8.0.4"
  }
}
